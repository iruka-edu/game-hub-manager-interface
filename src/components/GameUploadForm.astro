---
/**
 * Game Upload Form Component
 * Handles zip file upload -> metadata update -> GCS upload -> server update
 */
export interface Props {
  meta: {
    grade: string;
    subject: string;
    lesson: string[];
    level: string;
    skills: string[];
    themes: string[];
    linkGithub: string;
    quyenSach: string;
  };
}

const { meta } = Astro.props;
---

<div class="mt-6 space-y-6">
  <!-- Upload Section -->
  <div class="bg-white rounded-2xl border border-slate-200 shadow-sm p-6">
    <h2 class="text-lg font-bold text-slate-900 mb-4">Upload Game Files</h2>
    
    <!-- Upload Zone -->
    <div 
      id="uploadZone"
      class="border-2 border-dashed border-slate-300 rounded-xl p-8 text-center hover:border-slate-400 transition-colors cursor-pointer"
    >
      <div class="space-y-4">
        <div class="w-16 h-16 mx-auto bg-slate-100 rounded-full flex items-center justify-center">
          <svg class="w-8 h-8 text-slate-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 16a4 4 0 01-.88-7.903A5 5 0 1115.9 6L16 6a5 5 0 011 9.9M15 13l-3-3m0 0l-3 3m3-3v12"/>
          </svg>
        </div>
        <div>
          <p class="text-slate-600 font-medium">Kéo thả file ZIP vào đây hoặc</p>
          <label for="gameFile" class="inline-block mt-2 px-4 py-2 bg-blue-600 text-white rounded-lg cursor-pointer hover:bg-blue-700 transition-colors">
            Chọn file
          </label>
          <input type="file" id="gameFile" accept=".zip" class="hidden">
        </div>
        <p class="text-xs text-slate-400">Chỉ hỗ trợ file ZIP (tối đa 50MB)</p>
      </div>
    </div>

    <!-- Error Display -->
    <div id="uploadError" class="hidden mt-4 p-4 bg-red-50 border border-red-200 rounded-xl">
      <div class="flex items-start gap-3">
        <div class="flex-shrink-0">
          <svg class="w-5 h-5 text-red-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/>
          </svg>
        </div>
        <div class="flex-1">
          <h4 class="text-sm font-bold text-red-800" id="errorMessage">Đã xảy ra lỗi</h4>
          <p class="text-xs text-red-600 mt-1 hidden" id="errorDetails"></p>
        </div>
        <button type="button" onclick="document.getElementById('uploadError').classList.add('hidden')" class="text-red-400 hover:text-red-600">
          <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/>
          </svg>
        </button>
      </div>
    </div>

    <!-- Validation Results -->
    <div id="validationResults" class="hidden mt-4"></div>

    <!-- Upload Progress -->
    <div id="uploadProgress" class="hidden mt-4">
      <div class="bg-slate-200 rounded-full h-2">
        <div id="progressBar" class="bg-blue-600 h-2 rounded-full transition-all duration-300" style="width: 0%"></div>
      </div>
      <p id="progressText" class="text-sm text-slate-600 mt-2">Đang tải lên...</p>
    </div>

    <!-- Upload Status -->
    <div id="uploadStatus" class="hidden mt-4 p-4 bg-green-50 border border-green-200 rounded-lg">
      <div class="flex items-center gap-3">
        <div class="w-8 h-8 bg-green-100 rounded-full flex items-center justify-center">
          <svg class="w-4 h-4 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/>
          </svg>
        </div>
        <div class="flex-1">
          <p class="font-medium text-green-800">File đã upload thành công</p>
          <div class="text-sm text-green-600 space-x-4">
            <span>📁 <span id="fileName">-</span></span>
            <span>📊 <span id="fileSize">-</span></span>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- Manifest Configuration -->
  <div id="manifestSection" class="hidden bg-white rounded-2xl border border-slate-200 shadow-sm p-6">
    <div class="flex items-center justify-between mb-4">
      <h2 class="text-lg font-bold text-slate-900">Game Manifest</h2>
      <button id="editManifestBtn" class="text-sm text-blue-600 hover:text-blue-700 font-medium">
        Chỉnh sửa
      </button>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
      <div>
        <label class="block text-sm font-medium text-slate-700 mb-1">Game ID</label>
        <input 
          type="text" 
          id="gameIdInput"
          value=""
          class="w-full px-3 py-2 border border-slate-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500"
          placeholder="Sẽ được đọc từ manifest.json trong file ZIP"
        >
        <p class="text-xs text-slate-500 mt-1">Game ID sẽ tự động lấy từ manifest.json khi upload file ZIP</p>
      </div>
      
      <div>
        <label class="block text-sm font-medium text-slate-700 mb-1">Version</label>
        <input 
          type="text" 
          id="versionInput"
          value="1.0.0"
          class="w-full px-3 py-2 border border-slate-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500"
          placeholder="1.0.0"
        >
      </div>
      
      <div>
        <label class="block text-sm font-medium text-slate-700 mb-1">Runtime</label>
        <select 
          id="runtimeSelect"
          class="w-full px-3 py-2 border border-slate-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500"
        >
          <option value="HTML5">HTML5</option>
          <option value="Unity WebGL">Unity WebGL</option>
          <option value="Phaser">Phaser</option>
        </select>
      </div>
      
      <div>
        <label class="block text-sm font-medium text-slate-700 mb-1">Entry Point</label>
        <input 
          type="text" 
          id="entryInput"
          value="index.html"
          class="w-full px-3 py-2 border border-slate-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500"
          placeholder="index.html"
        >
      </div>
    </div>

    <!-- Metadata Display -->
    <div class="mt-4 p-4 bg-slate-50 rounded-lg">
      <h3 class="text-sm font-medium text-slate-700 mb-2">Metadata từ form</h3>
      <div class="grid grid-cols-2 md:grid-cols-4 gap-2 text-xs">
        <div><span class="font-medium">Lớp:</span> {meta.grade}</div>
        <div><span class="font-medium">Môn:</span> {meta.subject}</div>
        <div><span class="font-medium">Bài:</span> {meta.lesson.join(', ')}</div>
        <div><span class="font-medium">Level:</span> {meta.level}</div>
        <div class="col-span-2"><span class="font-medium">Skills:</span> {meta.skills.join(', ')}</div>
        <div class="col-span-2"><span class="font-medium">Themes:</span> {meta.themes.join(', ')}</div>
      </div>
    </div>
  </div>

  <!-- Thumbnail Upload Section -->
  <div id="thumbnailSection" class="hidden bg-white rounded-2xl border border-slate-200 shadow-sm p-6">
    <div class="flex items-center justify-between mb-4">
      <div>
        <h2 class="text-lg font-bold text-slate-900">Thumbnail Game</h2>
        <p class="text-xs text-slate-500 mt-1">Bắt buộc đủ 2 phiên bản: Desktop và Mobile</p>
      </div>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
      <!-- Desktop Thumbnail -->
      <div>
        <div class="flex items-center justify-between mb-2">
          <label class="text-sm font-bold text-slate-700">Desktop</label>
          <span class="text-xs text-slate-500">308 × 211 px</span>
        </div>
        <div 
          id="desktopDropZone"
          class="relative border-2 border-dashed border-slate-300 rounded-xl overflow-hidden cursor-pointer hover:border-slate-400 transition-colors"
          style="aspect-ratio: 308/211;"
        >
          <!-- Preview Image -->
          <img 
            id="desktopPreview" 
            class="hidden absolute inset-0 w-full h-full object-cover"
            alt="Desktop thumbnail preview"
          >
          <!-- Upload Placeholder -->
          <div id="desktopPlaceholder" class="absolute inset-0 flex flex-col items-center justify-center p-4">
            <svg class="w-10 h-10 text-slate-300 mb-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z"/>
            </svg>
            <p class="text-xs text-slate-400 text-center">Kéo thả hoặc click để chọn</p>
            <p class="text-[10px] text-slate-400">PNG, JPG (308×211)</p>
          </div>
          <input type="file" id="desktopFile" accept="image/png,image/jpeg,image/webp" class="hidden">
        </div>
        <!-- Validation Status -->
        <div id="desktopStatus" class="mt-2 hidden">
          <div id="desktopValid" class="hidden flex items-center gap-1 text-xs text-green-600">
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/>
            </svg>
            <span id="desktopValidText">Kích thước hợp lệ</span>
          </div>
          <div id="desktopInvalid" class="hidden flex items-center gap-1 text-xs text-red-600">
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/>
            </svg>
            <span id="desktopInvalidText">Kích thước không đúng</span>
          </div>
        </div>
      </div>

      <!-- Mobile Thumbnail -->
      <div>
        <div class="flex items-center justify-between mb-2">
          <label class="text-sm font-bold text-slate-700">Mobile</label>
          <span class="text-xs text-slate-500">343 × 170 px</span>
        </div>
        <div 
          id="mobileDropZone"
          class="relative border-2 border-dashed border-slate-300 rounded-xl overflow-hidden cursor-pointer hover:border-slate-400 transition-colors"
          style="aspect-ratio: 343/170;"
        >
          <!-- Preview Image -->
          <img 
            id="mobilePreview" 
            class="hidden absolute inset-0 w-full h-full object-cover"
            alt="Mobile thumbnail preview"
          >
          <!-- Upload Placeholder -->
          <div id="mobilePlaceholder" class="absolute inset-0 flex flex-col items-center justify-center p-4">
            <svg class="w-10 h-10 text-slate-300 mb-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z"/>
            </svg>
            <p class="text-xs text-slate-400 text-center">Kéo thả hoặc click để chọn</p>
            <p class="text-[10px] text-slate-400">PNG, JPG (343×170)</p>
          </div>
          <input type="file" id="mobileFile" accept="image/png,image/jpeg,image/webp" class="hidden">
        </div>
        <!-- Validation Status -->
        <div id="mobileStatus" class="mt-2 hidden">
          <div id="mobileValid" class="hidden flex items-center gap-1 text-xs text-green-600">
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/>
            </svg>
            <span id="mobileValidText">Kích thước hợp lệ</span>
          </div>
          <div id="mobileInvalid" class="hidden flex items-center gap-1 text-xs text-red-600">
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/>
            </svg>
            <span id="mobileInvalidText">Kích thước không đúng</span>
          </div>
        </div>
      </div>
    </div>

    <!-- Thumbnail Warning -->
    <div id="thumbnailWarning" class="hidden mt-4 p-3 bg-amber-50 border border-amber-200 rounded-lg">
      <div class="flex items-start gap-2">
        <svg class="w-5 h-5 text-amber-500 shrink-0 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"/>
        </svg>
        <div>
          <p class="text-sm font-medium text-amber-800">Cảnh báo kích thước</p>
          <p class="text-xs text-amber-700 mt-1" id="thumbnailWarningText">Một hoặc cả hai thumbnail không đúng kích thước khuyến nghị. Bạn vẫn có thể upload nhưng hình ảnh có thể bị méo.</p>
        </div>
      </div>
    </div>
  </div>

  <!-- Preview Section -->
  <div id="previewSection" class="hidden bg-white rounded-2xl border border-slate-200 shadow-sm p-6">
    <h2 class="text-lg font-bold text-slate-900 mb-4">Preview</h2>
    
    <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
      <!-- Game Info -->
      <div class="space-y-3">
        <div class="flex justify-between">
          <span class="text-sm text-slate-600">Game ID:</span>
          <span id="previewGameId" class="text-sm font-medium">-</span>
        </div>
        <div class="flex justify-between">
          <span class="text-sm text-slate-600">Version:</span>
          <span id="previewVersion" class="text-sm font-medium">-</span>
        </div>
        <div class="flex justify-between">
          <span class="text-sm text-slate-600">Runtime:</span>
          <span id="previewRuntime" class="text-sm font-medium">-</span>
        </div>
        <div class="flex justify-between">
          <span class="text-sm text-slate-600">Entry Point:</span>
          <span id="previewEntry" class="text-sm font-medium">-</span>
        </div>
        <div class="flex justify-between">
          <span class="text-sm text-slate-600">File Size:</span>
          <span id="previewSize" class="text-sm font-medium">-</span>
        </div>
      </div>

      <!-- Actions -->
      <div class="space-y-3">
        <button 
          id="validateBtn"
          class="w-full px-4 py-2 bg-yellow-600 text-white rounded-lg hover:bg-yellow-700 transition-colors"
        >
          🔍 Validate Game
        </button>
        
        <button 
          id="publishBtn"
          disabled
          class="w-full px-4 py-2 bg-green-600 text-white rounded-lg hover:bg-green-700 transition-colors disabled:bg-slate-300 disabled:cursor-not-allowed"
        >
          🚀 Publish Game
        </button>
      </div>
    </div>
  </div>
</div>

<script define:vars={{ meta }}>
  // Pass metadata to client-side script
  window.gameMeta = meta;
</script>