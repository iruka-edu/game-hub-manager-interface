---
interface Props {
  userRoles: string[];
}

const { userRoles } = Astro.props;

// Only show for admin users
const isAdmin = userRoles.includes('admin');
---

{isAdmin && (
  <div class="bg-white rounded-xl border border-slate-200 p-6">
    <div class="flex items-center justify-between mb-6">
      <div>
        <h3 class="text-lg font-semibold text-slate-900">Quản lý Google Cloud Storage</h3>
        <p class="text-sm text-slate-500 mt-1">Quản lý các file game trên Google Cloud Storage</p>
      </div>
      <div class="flex items-center gap-3">
        <button 
          id="refreshGCSBtn"
          class="px-4 py-2 text-sm font-medium text-slate-600 hover:text-slate-900 hover:bg-slate-100 border border-slate-300 rounded-lg transition-colors"
        >
          <svg class="w-4 h-4 mr-2 inline" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9m11 11v-5h-.581m0 0a8.003 8.003 0 01-15.357-2m15.357 2H15"></path>
          </svg>
          Làm mới
        </button>
        <button 
          id="forceRefreshGCSBtn"
          class="px-4 py-2 text-sm font-medium text-blue-600 hover:text-blue-800 hover:bg-blue-50 border border-blue-300 rounded-lg transition-colors"
        >
          <svg class="w-4 h-4 mr-2 inline" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9m11 11v-5h-.581m0 0a8.003 8.003 0 01-15.357-2m15.357 2H15"></path>
          </svg>
          Tải lại từ GCS
        </button>
        <button 
          id="deleteSelectedGamesBtn"
          class="px-4 py-2 text-sm font-medium text-orange-600 hover:text-orange-800 hover:bg-orange-50 border border-orange-300 rounded-lg transition-colors"
        >
          <svg class="w-4 h-4 mr-2 inline" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16"></path>
          </svg>
          Xóa game đã chọn
        </button>
        <button 
          id="cleanupGCSBtn"
          class="px-4 py-2 text-sm font-medium text-white bg-red-600 hover:bg-red-700 rounded-lg transition-colors"
        >
          <svg class="w-4 h-4 mr-2 inline" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16"></path>
          </svg>
          Dọn dẹp file thừa
        </button>
      </div>
    </div>

    <!-- Loading State -->
    <div id="gcsLoading" class="hidden text-center py-8">
      <div class="inline-flex items-center px-4 py-2 font-semibold leading-6 text-sm shadow rounded-md text-slate-500 bg-white">
        <svg class="animate-spin -ml-1 mr-3 h-5 w-5 text-slate-500" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24">
          <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle>
          <path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path>
        </svg>
        Đang tải dữ liệu GCS...
      </div>
    </div>

    <!-- Cache Status -->
    <div id="cacheStatus" class="mb-4 p-3 bg-blue-50 border border-blue-200 rounded-lg hidden">
      <div class="flex items-center gap-2 text-sm text-blue-800">
        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
        </svg>
        <span id="cacheStatusText">Dữ liệu từ cache</span>
      </div>
    </div>

    <!-- GCS Stats -->
    <div id="gcsStats" class="grid grid-cols-1 md:grid-cols-3 gap-4 mb-6 hidden">
      <div class="bg-blue-50 border border-blue-200 rounded-lg p-4">
        <div class="flex items-center">
          <div class="shrink-0">
            <svg class="w-8 h-8 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 7v10a2 2 0 002 2h14a2 2 0 002-2V9a2 2 0 00-2-2H5a2 2 0 00-2-2z"></path>
            </svg>
          </div>
          <div class="ml-4">
            <p class="text-sm font-medium text-blue-900">Tổng file trên GCS</p>
            <p id="totalGCSFiles" class="text-2xl font-semibold text-blue-600">-</p>
          </div>
        </div>
      </div>

      <div class="bg-green-50 border border-green-200 rounded-lg p-4">
        <div class="flex items-center">
          <div class="shrink-0">
            <svg class="w-8 h-8 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
            </svg>
          </div>
          <div class="ml-4">
            <p class="text-sm font-medium text-green-900">Game có trong DB</p>
            <p id="validGCSFiles" class="text-2xl font-semibold text-green-600">-</p>
          </div>
        </div>
      </div>

      <div class="bg-red-50 border border-red-200 rounded-lg p-4">
        <div class="flex items-center">
          <div class="shrink-0">
            <svg class="w-8 h-8 text-red-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"></path>
            </svg>
          </div>
          <div class="ml-4">
            <p class="text-sm font-medium text-red-900">File thừa (cần xóa)</p>
            <p id="orphanedGCSFiles" class="text-2xl font-semibold text-red-600">-</p>
          </div>
        </div>
      </div>
    </div>

    <!-- GCS Files List -->
    <div id="gcsFilesList" class="hidden">
      <div class="flex items-center justify-between mb-4">
        <h4 class="text-md font-medium text-slate-900">Danh sách file trên GCS</h4>
        <div class="flex items-center gap-2">
          <select id="gcsFilter" class="text-sm border border-slate-300 rounded-lg px-3 py-1.5 focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500">
            <option value="all">Tất cả file</option>
            <option value="valid">File hợp lệ</option>
            <option value="orphaned">File thừa</option>
          </select>
        </div>
      </div>

      <div class="bg-slate-50 rounded-lg overflow-hidden">
        <table class="w-full">
          <thead class="bg-slate-100 border-b border-slate-200">
            <tr>
              <th class="text-left px-4 py-3 w-12">
                <input 
                  type="checkbox" 
                  id="selectAllGCSFiles"
                  class="rounded border-slate-300 text-indigo-600 focus:ring-indigo-500"
                />
              </th>
              <th class="text-left px-4 py-3 text-sm font-semibold text-slate-700">Game ID</th>
              <th class="text-left px-4 py-3 text-sm font-semibold text-slate-700">Đường dẫn</th>
              <th class="text-left px-4 py-3 text-sm font-semibold text-slate-700">Kích thước</th>
              <th class="text-left px-4 py-3 text-sm font-semibold text-slate-700">Cập nhật</th>
              <th class="text-left px-4 py-3 text-sm font-semibold text-slate-700">Trạng thái</th>
            </tr>
          </thead>
          <tbody id="gcsFilesTableBody" class="divide-y divide-slate-200">
            <!-- Files will be populated here -->
          </tbody>
        </table>
      </div>
    </div>

    <!-- Empty State -->
    <div id="gcsEmptyState" class="hidden text-center py-8">
      <svg class="w-12 h-12 text-slate-400 mx-auto mb-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 7v10a2 2 0 002 2h14a2 2 0 002-2V9a2 2 0 00-2-2H5a2 2 0 00-2-2z"></path>
      </svg>
      <p class="text-slate-500">Không có file nào trên Google Cloud Storage</p>
    </div>
  </div>

  <!-- Delete Game Confirmation Modal -->
  <div id="deleteGameModal" class="fixed inset-0 bg-black/50 z-50 hidden items-center justify-center">
    <div class="bg-white rounded-xl max-w-lg w-full mx-4 p-6">
      <div class="flex items-center gap-3 mb-4">
        <div class="w-10 h-10 bg-orange-100 rounded-full flex items-center justify-center">
          <svg class="w-5 h-5 text-orange-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16"></path>
          </svg>
        </div>
        <h3 class="text-lg font-semibold text-slate-900">Xác nhận xóa game từ GCS</h3>
      </div>
      
      <p class="text-slate-600 mb-4">
        Bạn có chắc chắn muốn xóa <span id="deleteGameCount" class="font-medium text-orange-600">0</span> game(s) từ Google Cloud Storage?
      </p>
      
      <div class="bg-blue-50 border border-blue-200 rounded-lg p-4 mb-4">
        <div class="flex gap-3">
          <svg class="w-5 h-5 text-blue-600 shrink-0 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
          </svg>
          <div class="text-sm text-blue-800">
            <p class="font-medium mb-1">Lưu ý:</p>
            <ul class="list-disc list-inside space-y-1">
              <li>Chỉ xóa file trên Google Cloud Storage</li>
              <li>Dữ liệu game trong MongoDB sẽ được giữ nguyên</li>
              <li>Game vẫn hiển thị trong danh sách nhưng không có file</li>
            </ul>
          </div>
        </div>
      </div>
      
      <div id="deleteGamesList" class="max-h-32 overflow-y-auto mb-6 space-y-1 bg-slate-50 rounded-lg p-3">
        <!-- Games to be deleted will be listed here -->
      </div>
      
      <div class="flex items-center justify-end gap-3">
        <button id="cancelDeleteGame" class="px-4 py-2 text-sm font-medium text-slate-600 hover:text-slate-900 hover:bg-slate-100 border border-slate-300 rounded-lg transition-colors">Hủy</button>
        <button id="confirmDeleteGame" class="px-4 py-2 text-sm font-medium text-white bg-orange-600 hover:bg-orange-700 rounded-lg transition-colors">Xác nhận xóa</button>
      </div>
    </div>
  </div>

  <!-- Cleanup Confirmation Modal -->
  <div id="cleanupGCSModal" class="fixed inset-0 bg-black/50 z-50 hidden items-center justify-center">
    <div class="bg-white rounded-xl max-w-lg w-full mx-4 p-6">
      <div class="flex items-center gap-3 mb-4">
        <div class="w-10 h-10 bg-red-100 rounded-full flex items-center justify-center">
          <svg class="w-5 h-5 text-red-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"></path>
          </svg>
        </div>
        <h3 class="text-lg font-semibold text-slate-900">Xác nhận dọn dẹp file thừa</h3>
      </div>
      
      <p class="text-slate-600 mb-4">
        Bạn có chắc chắn muốn xóa <span id="cleanupCount" class="font-medium text-red-600">0</span> file thừa trên Google Cloud Storage?
      </p>
      
      <div class="bg-amber-50 border border-amber-200 rounded-lg p-4 mb-4">
        <div class="flex gap-3">
          <svg class="w-5 h-5 text-amber-600 shrink-0 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"></path>
          </svg>
          <div class="text-sm text-amber-800">
            <p class="font-medium mb-1">Lưu ý quan trọng:</p>
            <ul class="list-disc list-inside space-y-1">
              <li>Chỉ xóa các file không có game tương ứng trong database</li>
              <li>Các file của game còn tồn tại sẽ được giữ lại</li>
              <li>Hành động này không thể hoàn tác</li>
            </ul>
          </div>
        </div>
      </div>
      
      <div id="cleanupFilesList" class="max-h-32 overflow-y-auto mb-6 space-y-1 bg-slate-50 rounded-lg p-3">
        <!-- Files to be deleted will be listed here -->
      </div>
      
      <div class="flex items-center justify-end gap-3">
        <button id="cancelCleanup" class="px-4 py-2 text-sm font-medium text-slate-600 hover:text-slate-900 hover:bg-slate-100 border border-slate-300 rounded-lg transition-colors">Hủy</button>
        <button id="confirmCleanup" class="px-4 py-2 text-sm font-medium text-white bg-red-600 hover:bg-red-700 rounded-lg transition-colors">Xác nhận xóa</button>
      </div>
    </div>
  </div>
)}